# This is a Databricks asset bundle definition for cargo_fleet_management.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: cargo_fleet_management
  uuid: 8bf55fb1-5373-439f-8a2f-e8760e77b166

include:
  - resources/*.yml
  - resources/*/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-0febf608-6298.cloud.databricks.com
    variables:
      catalog: workspace
      schema: dev
    presets:
      artifacts_dynamic_version: true
  prod:
    mode: production
    workspace:
      host: https://dbc-0febf608-6298.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/subbuthiagu786@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/subbuthiagu786@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: workspace
      schema: prod
    permissions:
      - user_name: subbuthiagu786@gmail.com
        level: CAN_MANAGE
